# CLAUDE.md — 파인다이닝 씨어터

Claude Code가 이 저장소에서 작업할 때 따르는 규칙. 코드를 쓰기 전에 먼저 읽는다.
근거 문서는 예술경영지원센터 제출용 「웹·앱 개발 기능요구서」(**2026-08-21 판**)다. 요구서와 이 문서가 충돌하면 **요구서가 이긴다**. 발견하면 조용히 따르지 말고 지적한다.

---

## 1. 프로젝트

**파인다이닝 씨어터** — 공연IP와 미식을 결합한 콘텐츠를 제작·개발·유통하는 기업의 고객 서비스용 반응형 웹사이트.

- **서비스 형태**: 반응형 웹 (PC + Mobile). 네이티브/하이브리드 앱은 범위 밖.
- **목표 오픈**: 2026년 9월 21일 / **사업 종료**: 2026년 11월 13일
- **핵심 목적**: ① 기업과 보유 IP를 소개한다 ② B2C·B2B·B2G 고객을 회차 정보까지 데려간다 ③ 예약은 네이버 예약으로 넘긴다
- **필수 산출물**: 개발 완료 보고서, **운영 매뉴얼**

> 운영 매뉴얼이 필수 산출물이다. 편집 모드(§3.9)의 사용법이 곧 매뉴얼의 본문이므로, **화면을 만들 때마다 매뉴얼에 들어갈 조작 흐름을 함께 정리한다.** 마지막에 몰아서 쓰면 화면과 어긋난다.

이 사이트를 규정하는 두 가지 사실:

1. **결제하지 않는다.** 전환의 마지막 단계가 외부(네이버 예약)에 있다. 따라서 성능 지표는 매출이 아니라 **"예약 페이지로 넘어간 클릭"** 이다.
2. **일반 회원과 관리자가 같은 사이트에서 로그인한다.** 별도 어드민 사이트를 만들지 않고 **공개 사이트 위에서 편집 모드로 동작**한다. 다만 **인증 수단은 서로 다르다**(2026-08-27 확정) — 일반 회원은 카카오 소셜 로그인으로 가입하고, 관리자는 회원가입 절차 없이 **별도 테이블에 아이디·비밀번호를 미리 발급받아 그걸로 바로 로그인**한다(§3.1).

### 사용자 유형

**가입 유형(권한) 하나뿐이다.** 2026-08-26 판까지는 여기에 회원 유형(`AccountType`: 관람객/예술인/기업)이라는 별도 축이 있었으나, **2026-08-27 결정으로 완전히 제거했다** — 요구서 쪽도 함께 정리되었다(§15). 계정을 구분하는 축은 `Role` 하나뿐이다.

**가입 유형 = `Role` (무엇을 할 수 있는가)**

| 요구서 유형 | 역할 | 설명 |
|---|---|---|
| 일반회원 | `MEMBER` | 리뷰 작성, 협업 제안 발송 |
| 관리자 | `EDITOR` / `SUPER_ADMIN` | 포트폴리오·회차 등록·수정·발행. **카카오 가입과 무관한 별도 계정**(§3.1) — `SUPER_ADMIN`이 편집 모드에서 아이디·비밀번호를 직접 발급 |
| (비로그인 방문자) | — | 작품·회차·소개 열람과 예약 이동은 **로그인 없이 전부 가능** |

**열람에 로그인을 요구하지 않는다.** 관광객에게 카카오 로그인을 요구하면 그대로 이탈한다. 로그인이 필요한 지점은 리뷰 작성, 협업 제안 발송 두 곳뿐이다. 작품·회차·소개·**아티스트** 열람은 전부 로그인 없이 가능하다 — 아티스트 하트(좋아요)를 범위에서 뺐으므로(§3.8) 아티스트 페이지에는 더 이상 로그인 종속 기능이 없다.

### 도메인 용어 (코드·UI 통일)

| 용어 | 코드 | 뜻 |
|---|---|---|
| 작품 / IP | `Production` | 공연+식사가 결합된 하나의 창작물. 최상위 콘텐츠 단위 |
| 회차 | `Showing` | 특정 일시·장소의 실제 공연 1회 |
| 코스 | `Course` | 회차 안에서 순서대로 진행되는 요리+장면 묶음 |
| 예약 연결 | `BookingLink` | 회차에 매핑된 네이버 예약 URL + 판매 상태 |
| 아티스트 | `Artist` | 창작자 프로필. 계정과 별개다 (§3.8) |
| 리뷰 | `Review` | 회원이 쓰는 게시글. **요구서(기능7)가 부르는 이름은 '커뮤니티'** — 같은 기능이고 사이트 공개 명칭만 '리뷰'다 (§3.6) |
| 계정 | `Account` | 카카오로 로그인하는 일반 회원 신원. 역할 개념 없음(로그인 자체가 곧 `MEMBER` 자격) |
| 관리자 계정 | `AdminAccount` | 아이디·비밀번호로 로그인하는 관리자 신원. **`Account`와 별개 엔티티**(2026-08-27 확정, §3.1) |
| 역할 | `Role` | `AdminAccount`가 갖는 단일 값 — `EDITOR` / `SUPER_ADMIN`. `Account`(일반회원)에는 없다 |

`Product`, `Item`, `Order`, `Cart` 같은 커머스 명사를 쓰지 않는다. 우리는 아무것도 팔지 않는다.

**일반 회원 엔티티(`Account`)를 `Operator`나 `Admin`으로 이름 짓지 않는다.** 관리자 전용 엔티티는 `AdminAccount`로 따로 있으니(2026-08-27 확정) 이름이 헷갈릴 이유가 없다 — `Account`는 카카오로 로그인하는 일반 회원만 가리킨다. `User`도 쓰지 않는다 — MySQL 예약어와 겹치고 의미가 너무 넓다.

**`Artist`는 `Account`가 아니다.** 요구서의 아티스트는 프로필로 소개되는 대상이지 로그인하는 주체가 아니다. 아티스트가 직접 로그인하는 기능은 요구서에 없다. 두 개를 하나로 합치면 나중에 되돌리기 어렵다.

---

## 2. 기능 범위

요구서 「핵심 기능 요구사항」의 8개 항목이 과업이다. MVP 점검 기준상 **사전 협의 없는 임의 기능 축소는 불가**하고, "개발 중인 시스템" 상태는 완료로 인정되지 않는다. 절반만 만든 기능 8개보다 끝까지 도는 기능 7개가 낫지만, **빼려면 문서에서 먼저 빼야 한다.**

| # | 요구서 메뉴 | 완료 정의 |
|---|---|---|
| 1 | 회원가입 / 소셜로그인 | 카카오 연동 가입. 회원 유형 선택 없음(2026-08-27 제거). 관리자는 카카오와 무관한 별도 아이디·비밀번호 계정으로 로그인, 재방문 시 PIN 진입(§3.1·§3.4) |
| 2 | 포트폴리오 / 작품 업로드 | 관리자가 고해상도 이미지(≤20MB)+설명(텍스트)+캡션 등록·수정·삭제 → 변환 → 목록·상세 노출 |
| 3 | 포트폴리오 / 영상 기록물 | 유튜브 임베드 영상 재생 + 고해상도 이미지 확대·축소(딥줌) |
| 4 | 협업제안 | 회원이 제안서 발송 → **관리자가 로그인 후 편집 모드에서 목록 확인**/수락/거절 → 발송자 알림 (§3.7) |
| 5 | 예약 | 회차 일정 확인 → **네이버 예약 외부 연결** (§4) |
| 6 | 아티스트 | 창작자 프로필 노출 + 모집 공고 열람. 프로필은 **관리자가 편집 모드에서 등록·수정·삭제**(§3.8). 하트(좋아요)는 범위에서 제외 — 2026-08-26 결정 |
| 7 | 커뮤니티 | 로그인 회원의 게시글 작성·조회 + 최소 운영 기능(§3.6). **사이트 공개 명칭은 '리뷰'** — 요구서상 이름과 사이트 UI 이름이 다를 뿐 같은 기능이다. 관리자는 숨김·삭제뿐 아니라 **원문 수정**도 가능(2026-08-26 결정) |
| 8 | AI 챗봇 (RAG) | **차후 과제로 보류** — §16 참조. 보류 상태를 요구서와 맞추는 것이 선행 조건 |
| + | 다국어 | 한국어 / 영어. 과업내용에는 있으나 **핵심 기능 표에 누락** — §15 참조 |

### 범위 밖 — 구현하지 않는다

자체 결제, 좌석 선점·정원 차감, 푸시 알림, 네이티브/하이브리드 앱, 아티스트 셀프 가입 및 셀프 업로드, 오디션 지원 접수(공고 열람까지만), **아티스트 하트(좋아요)**(2026-08-26 제외 — §3.8).

### 우선순위

오픈일(9/21)까지 전부 완성되지 않을 때 **무엇을 먼저 포기할지는 개발자가 정하지 않는다.** 요구서 변경은 센터 사전 승인 사항이다. 일정이 밀릴 조짐이 보이면 코드를 줄이기 전에 문서를 고치고 협의한다.

다만 구현 순서는 의존성으로 정해진다.

```
1) 콘텐츠 기반   : 작품·회차·소개·아티스트 + 편집 모드 + 이미지 파이프라인 (기능 2·3·6)
2) 유입 완결     : 회차 캘린더 + 네이버 예약 연결 + 다국어 (기능 5·다국어)
3) 계정 기반     : 카카오 로그인 + 역할 + PIN (기능 1)
4) 계정 의존 기능 : 리뷰(요구서상 커뮤니티), 협업제안 (기능 7·4)
5) 보류          : RAG 챗봇 (기능 8)
```

**아티스트가 1단계로 옮겨졌다(2026-08-26).** 원래 4단계에 있었던 이유는 하트(좋아요)가 로그인을 요구했기 때문인데, 하트를 범위에서 뺀 지금 아티스트 프로필은 작품·소개와 똑같이 "관리자가 편집 모드에서 등록하고 누구나 로그인 없이 본다"는 콘텐츠일 뿐이다. 계정 시스템(3단계)보다 먼저 만들 수 있고, 먼저 만들어야 한다.

1·2단계만으로도 사이트는 **공개 가능한 완성품**이다(아티스트 포함). 3단계가 늦어져도 방문자는 아무 불편이 없다. 이 순서를 뒤집어 로그인부터 만들면, 일정이 밀렸을 때 보여줄 게 로그인 화면뿐이다.

---

## 3. 계정과 권한

일반 회원과 관리자는 **같은 사이트**를 쓰지만, **로그인 수단은 서로 다르다**(2026-08-27 확정) — 회원은 카카오, 관리자는 아이디·비밀번호. 로그인 이후의 세션 형태(§7.4)와 권한 검사(§3.3)만 한 갈래로 수렴한다.

### 3.1 계정 모델

요구서는 **일반 회원의 카카오 연동 가입**만 명시한다. 구글·애플은 요구사항이 아니므로 구현하지 않는다 (애플 로그인의 ES256 client secret·private relay 처리가 통째로 사라진다 — 일정상 큰 이득이다). 다만 **관광객 타겟과 충돌한다**는 점은 §15에 남긴다.

**계정은 두 개의 완전히 별개인 엔티티다.** 일반 회원과 관리자는 가입 경로·인증 수단·테이블이 전부 다르다. 관리자에게 회원가입 절차는 없다 — `SUPER_ADMIN`이 편집 모드에서 아이디·비밀번호를 직접 지정해 계정을 만든다.

```
account            -- 카카오로 가입하는 일반 회원
  id, provider, provider_user_id, email(nullable), nickname,
  locale, status, created_at

admin_account      -- 관리자. 회원가입 절차 없음, SUPER_ADMIN이 직접 발급
  id, username, password_hash, role, status, pin_hash, created_at
```

- **`Role`은 `admin_account`에만 있는 단일 값이다** (`EDITOR` | `SUPER_ADMIN`). `Set<Role>`이 아니다 — 관리자와 일반회원이 이제 다른 테이블이므로 "일반회원이면서 에디터"인 사람 자체가 존재할 수 없다. `account`(일반회원)에는 role 컬럼이 없다 — 로그인 자체가 곧 리뷰 작성·협업제안 발송 자격이다.
- **회원 유형(`AccountType`: 관람객/예술인/기업) 축은 없다(2026-08-27 제거).** 계정을 구분하는 축은 `Role`(관리자 쪽) 하나뿐이고, 그마저도 이제 별개 테이블이다.
- **관리자 승격·초대 경로는 없다(2026-08-27 폐기).** 기존 카카오 회원 계정을 `EDITOR`로 승격시키는 길이 없다 — 관리자는 언제나 `admin_account`에만 존재한다. `account_invite` 테이블은 만들지 않는다.
- 최초 `SUPER_ADMIN` 1건은 Flyway 마이그레이션으로 심는다. 이후 추가 관리자 계정은 **`SUPER_ADMIN`이 편집 모드의 "관리자 계정 관리" 화면에서 직접 생성**한다(PIN sudo 모드로 보호, §3.4) — 별도 관리 API·화면을 이번 관리자 로그인 작업 범위에 포함한다.
- 비밀번호는 BCrypt 해시로 저장한다. 평문·양방향 암호화 금지(PIN과 동일 원칙, §3.4). 시드로 심은 최초 비밀번호를 관리자가 로그인 후 직접 바꿀 수 있어야 한다 — 영구히 쓰게 두지 않는다.
- **`Artist` 엔티티는 어느 쪽 계정과도 별개다.** 사이트에 소개되는 창작자 프로필(§3.8)이며, 로그인 계정이 그걸 자동으로 갖지 않는다.
- **`account.status`는 최소한 `ACTIVE | WITHDRAWN`을 포함한다.** 탈퇴는 물리 삭제가 아니라 상태 전환이다(§3.2) — `Review.account_id` 같은 참조가 끊기지 않게 하기 위해서다. `admin_account.status`는 `ACTIVE | DISABLED` 정도로 단순하게 — 관리자는 탈퇴 개념이 없고 `SUPER_ADMIN`이 비활성화한다.

### 3.2 가입 정책은 코드가 아니라 설정이다

**이 섹션은 일반 회원(`account`, 카카오) 가입에만 적용된다.** 관리자(`admin_account`)는 가입 절차 자체가 없으므로(§3.1) 아래 정책과 무관하다.

```yaml
signup:
  policy: OPEN        # 리뷰(요구서상 커뮤니티)가 필수 기능이므로 1차 오픈부터 개방
  local-login: false  # prod=false, dev=true — 일반회원 점검 계정 전용(§13.9). 관리자 점검 계정은 admin_account가 항상 제공하므로 이 플래그와 무관
```

- `OPEN`: 카카오 인증을 통과하면 계정이 생긴다. 회원 유형 선택 화면 없음(2026-08-27 제거).
- `INVITE_ONLY`: `signup_allowlist`(provider, provider_user_id 또는 email)에 없으면 계정을 만들지 않는다. 지금은 쓰지 않지만 분기는 남긴다 — 리뷰 스팸이 터지면 즉시 잠글 수 있는 유일한 스위치다. **관리자와는 무관한 별도 테이블이다** — 헷갈리지 않게 `account_invite`라는 이름은 다시 쓰지 않는다.
- **이 분기는 `SignupPolicy` 한 곳에만 존재한다.** OAuth2 `SuccessHandler`가 여기에 물어본다.

**가입을 여는 순간 필수가 되는 것들** (코드보다 먼저 준비해야 한다)
- 이용약관, 개인정보처리방침 (수집 항목·목적·보유기간)
- **회원 탈퇴 경로와 데이터 삭제 처리.** **탈퇴해도 리뷰 게시글은 삭제하지 않고 그대로 남긴다**(2026-08-26 확정). 탈퇴는 계정을 물리 삭제하는 게 아니라 `account.status`를 `WITHDRAWN`으로 바꾸는 것이다(§3.1) — 계정 행 자체를 지우면 `Review.account_id` 참조가 끊겨 글이 고아가 되거나 같이 사라진다. 대신 개인정보만 지운다: `nickname`은 `탈퇴한 회원`으로 치환, `email`·`provider_user_id`는 익명화(null 또는 되돌릴 수 없는 해시)한다. 화면에는 작성자가 `탈퇴한 회원`으로 표시되고 글 본문·작성일은 그대로 보인다. 탈퇴한 계정은 재로그인할 수 없다 — 카카오로 다시 로그인하면 `provider_user_id`가 지워졌으니 새 계정으로 취급된다.
- 만 14세 미만 가입 처리 방침
- 이건 개발이 아니라 문서 작업이고, **없이는 오픈할 수 없다.**

### 3.3 권한 검사 규칙

- **역할이 아니라 능력(capability)으로 검사한다.** `hasRole('EDITOR')`까지는 쓰되, 서비스 안에서 `if (role == EDITOR)` 같은 동등 비교를 하지 않는다.
- 권한 판정은 `@PreAuthorize`에만 둔다. 서비스 본문에 권한 분기를 넣지 않는다.
- **소유권 검사는 권한 검사와 별개다.** 리뷰 글 수정·삭제는 `isAuthenticated()`(일반 회원 로그인 자체에 role이 없으므로, §3.1)로는 부족하고 "내 글인가"를 서비스에서 확인해야 한다. 이건 `@PreAuthorize`가 아니라 서비스 안에서, 예외는 `POST_NOT_OWNED`로 통일한다.
- 관리자는 남의 글을 수정·삭제할 수 있다. 소유권 검사를 우회하는 경로는 `EDITOR` 이상 하나뿐이고, 감사 로그(§7.7)에 반드시 남긴다.
- 콘텐츠 엔티티(`Production`, `Showing`)에 소유권을 넣지 않는다. 관리자가 올리는 것이고 작성자별 권한이 없다. 소유권이 필요한 건 `Review`와 `Proposal`뿐이다.

### 3.4 6자리 간편비밀번호 (PIN)

**PIN은 이미 인증된 관리자 세션의 재진입 + 민감 동작 재확인 수단**이다. `pin_hash`는 `admin_account`에만 있다 — 일반 회원(카카오)에게는 PIN이 없다.

- 세션 진입: 아이디·비밀번호 재인증 대신 PIN으로 편집 모드에 재진입한다.
- **파괴적·공개적 동작 전에 PIN을 다시 요구한다.** 작품 발행/발행 취소, 삭제, 예약 URL 변경, **관리자 계정 발급·역할 변경**(§3.1). PIN 통과 후 15분간 유효(`sudo` 모드).
- BCrypt 해시 저장. 평문·양방향 암호화 금지.
- 생년월일, `000000`, `123456`, 연속·동일 숫자 거부.
- 5회 실패 시 잠금 → 아이디·비밀번호 재로그인으로만 해제.
- **PIN 단독으로 새 세션을 만들 수 없다.** 이미 발급된 세션의 재진입·재확인 수단일 뿐이다.

### 3.5 별도 어드민을 만들지 않는다는 것의 실제 의미

가장 큰 위험은 **"UI를 숨겼으니 안전하다"** 는 착각이다. 편집 버튼이 화면에 안 보이는 것과 API가 막힌 것은 아무 관계가 없다.

- **권한은 100% 서버에서 강제한다.** 모든 쓰기 엔드포인트에 `@PreAuthorize("hasRole('EDITOR')")`. 프론트의 조건부 렌더링은 편의 기능일 뿐 보안 장치가 아니다.
- **presign 발급 엔드포인트가 이 시스템에서 가장 위험한 API다.** 인증이 없으면 누구나 우리 S3에 무제한 업로드할 수 있다. 운영자 인증 + 파일 크기·MIME 화이트리스트 + 계정당 발급 레이트리밋을 모두 건다.
- **편집 모드 코드는 익명 방문자에게 전송되지 않는다.** `features/editing/*`를 `React.lazy`로 분리하고, 인증된 세션에서만 import한다. 편집 UI가 초기 번들에 들어가면 사이트 성능(요구서가 강조한 지점)과 보안이 동시에 나빠진다.
- **정적 HTML에 편집 마크업이 남으면 안 된다.** 프리렌더 산출물은 익명 상태로 생성한다.
- 쓰기 요청에 **CSRF 방어**를 건다. refresh token이 쿠키에 있으므로 `SameSite=Lax`만으로는 부족하다. access token은 `Authorization` 헤더로 보내 쿠키 자동 전송에 의존하지 않는다.
- 로그인 진입점은 `/login` 하나. 이 한 페이지 안에 **카카오 로그인 버튼(일반 회원)과 아이디·비밀번호 폼(관리자)이 함께** 있다 — 화면은 하나, 인증 흐름은 둘. 네비게이션에 노출하지 않되(푸터에 작게), **경로를 숨겨서 지키려 하지 않는다.** `robots.txt`에서 `Disallow`, 메타 `noindex`.
- **관리자 로그인 폼은 무차별 대입에 노출된다.** 카카오는 OAuth라 이 위험이 없지만 아이디·비밀번호는 있다. 실패 횟수 기반 잠금(계정 단위) + IP당 레이트리밋을 건다. PIN 5회 잠금(§3.4)과는 별개의 방어선이다.
- 모든 쓰기에 감사 로그: 누가·언제·무엇을·이전값→이후값. 운영자가 소수라 사고가 나면 추적이 유일한 복구 수단이다.

### 3.6 리뷰 (기능 7, 요구서상 명칭: 커뮤니티) — 가장 과소평가되는 기능

**리뷰는 별개 기능이 아니다.** 요구서 기능 7 "커뮤니티"를 사이트에서 부르는 이름이 "리뷰"일 뿐, 게시판은 하나다(2026-08-26 확정 — 한 번은 이걸 놓치고 별도 `Review` 엔티티·§3.10을 새로 만들었다가 다시 합쳤다). 이 문서 전체에서 앞으로 "리뷰"라고 쓰면 요구서의 "커뮤니티"를 가리키는 것이다.

"게시판 하나"로 보이지만 **운영 부담이 가장 큰 기능**이다. 스팸과 부적절한 글은 반드시 온다. 오픈 후 며칠 안에 온다.

최소 구성 — 이보다 줄이지 않는다.

- `Review(account_id, title, body, status, created_at)`, `ReviewComment`
- `status: PUBLISHED | HIDDEN | DELETED`. **물리 삭제하지 않는다** — 신고 처리 이력이 남아야 한다.
- **작성자가 탈퇴해도 글은 지우지 않는다**(§3.1·§3.2). `account.status = WITHDRAWN`이 될 뿐 `account_id` 참조는 그대로 유지되고, 화면에는 작성자가 `탈퇴한 회원`으로 표시된다.
- 본인 글 수정·삭제 (소유권 검사, §3.3).
- **관리자(`EDITOR` 이상)는 남의 글을 숨김·삭제뿐 아니라 원문 수정까지 할 수 있다** (2026-08-26 결정 — 처음엔 커뮤니티와 똑같이 숨김·삭제까지만 열어뒀는데, 리뷰는 그보다 넓게 간다). 수정은 감사 로그에 `before`/`after` 본문을 반드시 남긴다(§7.7) — 운영자가 뭘 고쳤는지 나중에 추적 가능해야 한다.
- **신고 기능**(`ReviewReport`). 관리자 화면에 신고 목록. 신고 3건 이상이면 자동 `HIDDEN` 후 검토.
- 회원당 하루 게시글 상한(예: 10건), 연속 등록 간격 제한(30초). 스팸의 90%는 이걸로 막힌다.
- **본문은 서버에서 새니타이즈한다.** 리치 에디터를 쓰지 않는다 — 플레인 텍스트 + 줄바꿈 + 링크 자동 인식까지만. HTML 입력을 허용하면 XSS 표면이 열리고, 이 사이트의 콘텐츠 성격상 얻는 게 없다.
- 이미지 첨부는 **1차 범위에서 뺀다.** 회원 업로드는 저장 비용·불법 이미지·검열 책임을 한꺼번에 데려온다. 요구서에도 "게시글을 작성할 수 있음"까지만 있다.
- 리뷰 페이지는 **`noindex`**. 회원 글이 검색에 노출되면 개인정보·저작권 문제가 사이트 책임이 된다.
- 특정 작품에 묶지 않는다 — 요구서의 커뮤니티 그대로 **일반 게시판**이다. "이 작품에 대한 리뷰"처럼 작품별로 묶고 싶다면 그건 범위 확장이니 먼저 협의한다.
- `review/` 패키지: `ReviewController`(공개 조회) + `ReviewEditController`(작성자 본인 + 관리자, `@PreAuthorize`)(§6).

### 3.7 협업 제안 (기능 4)

- `Proposal(account_id, title, body, contact_email, attachments, status)`
- 상태: `SENT → READ → ACCEPTED | DECLINED`. 상태 변경 시 발송자에게 이메일 알림.
- 로그인 회원만 발송. 회원 유형 구분이 없으므로(§1·§3.1) 폼 자동 채움 로직도 없다 — 누구나 같은 폼을 쓴다.
- 회신용 이메일은 **폼에서 직접 입력받는다.** 카카오 이메일이 없을 수 있다(§7.4).
- 첨부는 PDF ≤10MB, 1건당 3개. 이미지 파이프라인(§7.5)을 태우지 않고 별도 prefix에 원본 그대로 둔다. 공개 URL을 부여하지 않고 관리자만 presigned GET으로 내려받는다.
- 회원당 일 3건 제한 + 허니팟.
- **관리자(`EDITOR` 이상)로 로그인해야 목록을 볼 수 있다.** 확인·수락/거절 화면은 편집 모드 안에 둔다 — 편집 모드 진입 자체가 로그인 조건이므로(§3.5) 별도 인증을 새로 만들지 않는다. 별도 어드민을 만들지 않는다.

### 3.8 아티스트 (기능 6)

- `Artist(name, role, bio, photo, links)` + `Production`과 N:M. **계정이 아니다**(§1).
- **하트(좋아요)는 범위에서 뺐다(2026-08-26).** `ArtistLike` 엔티티도, `/api/me/likes`도 만들지 않는다. 로그인 종속 기능이 사라졌으므로 아티스트 페이지는 작품·소개와 동일하게 완전히 공개다.
- **프로필 등록·수정·삭제는 관리자가 편집 모드에서 한다** — `Production`과 같은 패턴이다: `ArtistController`(공개 조회) + `ArtistEditController`(`@PreAuthorize`) + `Publishable`(DRAFT/PUBLISHED) 상속(§7.3). 초안 프로필이 목록에 새는 사고를 막으려면 `Production`과 똑같이 `status = PUBLISHED` 필터를 리포지토리 메서드 이름에 박는다(§7.3).
- 모집 공고 `Casting`은 **열람 전용**. 지원 접수는 범위 밖이며, 지원 방법은 본문에 외부 이메일·폼 링크로 안내한다.

### 3.9 편집 모드 (Edit Mode)

인라인 WYSIWYG(contenteditable)을 구현하지 않는다. 다국어 + 고해상도 이미지 파이프라인과 겹치면 감당할 수 없다. 대신 **같은 페이지, 편집 패널** 방식으로 간다.

```
공개 화면과 동일한 레이아웃
┌────────────────────────────────┬──────────────┐
│                                │ 편집 패널     │
│   실제 작품 상세 페이지          │ ─────────    │
│   (섹션에 hover 시 ✎ 표시)      │ KO  |  EN    │
│                                │ 제목 [     ] │
│   ┌──────────────┐             │ 설명 [     ] │
│   │ 이미지 드롭존  │             │ 캡션 [     ] │
│   └──────────────┘             │              │
│                                │ [임시저장]    │
│                                │ [발행] 🔒PIN │
└────────────────────────────────┴──────────────┘
```

- 상단에 편집 모드 토글. 켜면 섹션마다 편집 어피던스가 붙고, 우측 패널에서 수정한다.
- 패널은 **KO/EN을 탭으로 나란히** 둔다. 번역을 나중에 따로 하는 구조로 만들면 영문이 영구히 비어 있게 된다.
- 이미지는 드래그&드롭 → presign → S3 직접 업로드 → 파생본 생성 진행률 표시.
- **영상은 파일 업로드가 아니라 유튜브 URL(또는 video ID) 입력이다(§5).** 운영자가 자기 유튜브 채널에 먼저 올린 뒤 그 URL을 패널에 붙여넣는다. 우리 서버·S3는 영상 파일을 절대 받지 않는다 — presign 대상은 이미지뿐이다.
- **모든 콘텐츠에 `status: DRAFT | PUBLISHED`.** 편집은 초안에 하고, 발행 시 공개본으로 교체한다. 라이브 사이트에서 편집하므로 이게 없으면 작업 중인 문장이 그대로 방문자에게 보인다.
- 운영자는 `?preview=true`로 초안 상태를 볼 수 있다. 익명 요청에서 이 파라미터는 무시된다(서버에서 권한 확인).
- 발행 시: `@CacheEvict` + `sitemap.xml` 갱신 + 프리렌더 대상이면 재생성 트리거.
- **자동 저장하지 않는다.** 명시적 [임시저장]만. 라이브 사이트에서 의도치 않은 저장은 사고다.

---

## 4. 예약 = 외부 연결. 여기에 함정이 있다

요구서 참고 조항에 이렇게 적혀 있다.

> "단순 외부 쇼핑몰 연결 방식은 자체 구축으로 간주하기 어려우므로 유의"
> "외부 플랫폼(KLOOK, 야놀자 등) 이용 시 사업자명, 서비스명 등 검색 가능한 정보 명시"

즉 **버튼 하나에 네이버 예약 링크만 걸면 예약 기능을 구현하지 않은 것으로 판정될 수 있다.**

**우리가 자체 구축하는 것**
- `Showing` 엔티티 — 일시, 장소, 소요 시간, 정원, 코스 구성, 진행 언어, 판매 상태를 DB로 관리
- 회차 캘린더 UI — 월/주 단위 조회, 작품별·지점별·언어별 필터
- 회차 상세 — 코스 큐시트, 유의사항, 접근 정보
- 판매 상태 배지 — `OPEN | CLOSING_SOON | SOLD_OUT | ENDED`
- 편집 모드에서의 회차 등록·수정, 예약 URL 매핑, 상태 1클릭 토글
- **이탈 트래킹** — 예약 링크 클릭을 `booking_click`에 적재 (회차, 유입 경로, 언어, UTM). 사업 완료 보고서의 성과 지표가 된다

**하지 않는 것**
- 좌석 선점·정원 차감. 재고의 진실은 네이버 예약에 있다.
- **잔여석 숫자 표시 금지.** 네이버 예약은 공개 조회 API가 없어 실시간 동기화가 불가능하다. 숫자를 띄우면 반드시 불일치하고 현장 CS 사고가 된다. 상태 배지까지만.
- 예약 확정·취소·환불 처리. 전부 네이버 예약 쪽이다.

**구현 규칙**
- 링크는 `target="_blank" rel="noopener noreferrer"`.
- 외부로 나간다는 사실을 버튼에 밝힌다. "예약하기"가 아니라 **"네이버 예약으로 이동"** + 외부 링크 아이콘.
- 클릭 트래킹은 `POST /api/showings/{id}/booking-click`을 `navigator.sendBeacon`으로 보내고 **응답을 기다리지 않고 즉시 이동**시킨다. 트래킹 실패가 예약을 막아선 안 된다.
- 예약 URL이 비었거나 상태가 `SOLD_OUT`이면 버튼을 비활성화하고 다음 회차를 안내한다.
- 예약 URL은 저장 시 **호스트 화이트리스트로 검증**한다. 오타 하나로 예약이 통째로 죽는다.

---

## 5. 기술 스택 (고정)

**Backend**
- Java 17 / Spring Boot 3.5.x (jakarta.*)
- Spring Web MVC, Spring Data JPA, Spring Validation
- Spring Security — 일반 회원은 OAuth2 Client(카카오), 관리자는 자체 아이디·비밀번호 인증(폼 로그인 + `UserDetailsService`가 `admin_account` 조회). 둘 다 같은 모양의 세션/토큰으로 수렴(§3·§7.4). Method Security
- MySQL 8.x + Flyway
- Gradle Kotlin DSL

**Infra** (상세는 §13)
- Docker Compose 단일 스택 — nginx + backend (+ 로컬 전용 mysql override)
- AWS: EC2 (t4g, arm64) / CloudFront + ACM / S3 / RDS(dev·prod) / ECR / SSM
- CI/CD: GitHub Actions → ECR → SSM Run Command

**Frontend**
- React 19 + TypeScript 5.x + Vite
- React Router v7 / TanStack Query v5
- CSS Modules + CSS Custom Properties
- react-i18next (ko/en)
- OpenSeadragon (고해상도 딥줌)

**External**
- 스토리지: S3 + CloudFront
- 영상: **유튜브 임베드로 확정**(2026-08-26, Vimeo 후보 제외). 운영자가 유튜브 채널에 업로드한 영상을 편집 패널에서 URL로 연결해 재생한다(§3.9). **원본 영상 파일을 우리 서버·S3로 직접 받거나 서빙하지 않는다** (전송량 비용 + 트랜스코딩 부담이 통째로 사라진다)
- 메일: SES 또는 Resend (협업 문의 접수 알림)
- 예약: 네이버 예약 (외부 링크. SDK·API 연동 없음)

**도입하지 않는다**
- PG·결제 SDK 일체.
- **Redis.** 이 규모(작품 수십, 회차 수백, 동시 쓰기 없음, 운영자 5명)에서 캐시 계층은 순수 부채다. MySQL + Spring Cache(Caffeine)로 충분하다.
- 헤드리스 CMS(Strapi, Contentful 등). 편집 기능이 요구서의 핵심 과업이고 **소스코드 원본을 산출물로 제출**해야 한다. 외부 CMS에 콘텐츠 모델이 있으면 자체 구축으로 인정받기 어렵다.
- Firebase Auth, Supabase 등 백엔드 대체 SaaS. 같은 이유.
- FCM·푸시 인프라. 알림은 이메일만.
- Redux / Zustand / MobX. 서버 상태는 TanStack Query, UI 상태는 `useState`+Context.
- Tailwind / styled-components / MUI / Ant Design. 아트디렉션이 강해서 기본값이 방해된다.
- Java 21+ 문법(virtual threads, switch 패턴 매칭 preview). 런타임이 17이다.
- Lombok은 `@Getter` / `@RequiredArgsConstructor`만. `@Data`, `@Setter`, `@Builder` 남용 금지.

---

## 6. 디렉토리

```
backend/src/main/java/com/finediningtheater/
├── global/
│   ├── config/          # WebMvc, Jpa, Security, Cors, Cache
│   ├── security/        # OAuth2 핸들러, JwtProvider, PinAuth, SudoMode
│   ├── audit/           # 감사 로그
│   ├── error/           # ErrorCode, BusinessException, GlobalExceptionHandler
│   ├── response/        # ApiResponse, PageResponse
│   └── support/         # BaseTimeEntity, Publishable, 유틸
├── account/             # 일반 회원 계정(카카오), 관리자 계정(아이디·비밀번호, 별개 엔티티), PIN, 가입 정책
├── production/          # 작품(IP), 이미지, 영상, 코스, 번역
│   ├── ProductionController.java        # 공개 조회
│   ├── ProductionEditController.java    # 쓰기. @PreAuthorize
│   └── ...
├── showing/             # 회차, 캘린더, 예약 링크, 클릭 트래킹
├── about/               # 집단 소개 콘텐츠, 연혁
├── artist/              # 아티스트 프로필, 모집 공고(Casting). ArtistEditController는 관리자 전용(§3.8)
├── review/              # 리뷰(요구서상 커뮤니티, 기능7). 작성자 본인 + 관리자 모두 쓰는 ReviewEditController(§3.6)
├── inquiry/             # 협업 문의 (비로그인 폼)
└── media/               # 업로드 presign, 파생본 생성

frontend/src/
├── styles/tokens.css    # 디자인 토큰 단일 진실 공급원
├── locales/{ko,en}/
├── api/
├── hooks/               # useReveal, useMediaQuery, useDeepZoom
├── components/{layout,primitives,section}/
├── pages/               # 공개 페이지
└── features/editing/    # 편집 모드 전용. React.lazy로만 진입
```

**별도 `admin/` 패키지를 만들지 않는다.** 각 도메인이 자기 쓰기 컨트롤러(`XEditController`)를 갖는다. 어드민 사이트가 없으므로 어드민 패키지도 없다. 대신 쓰기 컨트롤러는 파일명으로 구분되어야 한다 — 권한 검사가 빠진 곳을 리뷰에서 바로 찾을 수 있게.

도메인 패키지 내부는 `XController / XEditController / XService / XRepository / X(entity) / dto/`.

---

## 7. 백엔드 규칙

### 7.1 계층

- **Controller**: `@Valid` 검증, DTO 변환 위임, HTTP 관심사만. 비즈니스 분기 금지.
- **Service**: 트랜잭션 경계. 클래스에 `@Transactional(readOnly = true)`, 쓰기 메서드에만 `@Transactional` 재선언.
- **엔티티는 컨트롤러 밖으로 나가지 않는다.** 응답은 항상 DTO.
- 요청/응답 DTO는 **record**. 변환은 `static X from(Entity e)` 정적 팩토리. MapStruct 안 씀.
- **모든 `*EditController`의 모든 메서드에 `@PreAuthorize`가 있어야 한다.** 클래스 레벨에 걸고 메서드에서 필요시 강화한다. 없는 메서드는 리뷰에서 반려.
- **공개 조회 API는 인증을 옵셔널로 설계한다.** `@AuthenticationPrincipal`을 nullable로 받고, 로그인 여부로 응답 구조가 갈리지 않게 한다. 이걸 지키면 나중에 뷰어 종속 기능이 붙어도 컨트롤러 시그니처를 고치지 않는다.

### 7.2 응답 / 예외 / 뷰어 종속 데이터

**공개 응답 DTO에 보는 사람에 따라 달라지는 값을 섞지 않는다.** `ProductionDetailResponse`에 `isLiked`, `myReview` 같은 필드를 넣는 순간 응답이 캐시 불가능해지고, 캐싱은 이 사이트의 성능 요구사항이다.

- 뷰어 무관 데이터: `GET /api/productions/{slug}` → 캐시 가능
- 뷰어 종속 데이터: `GET /api/me/productions/{slug}` → 캐시 불가, 인증 필요

지금 두 번째 엔드포인트는 존재하지 않는다. 만들지 않는다. 다만 첫 번째 DTO를 뷰어 무관으로 유지하는 것만으로 나중에 두 번째를 추가하는 비용이 0이 된다. **이게 §2에서 말한 세 번째 이음새다.**

```json
{ "success": true,  "data": {}, "error": null }
{ "success": false, "data": null, "error": { "code": "PIN_REQUIRED", "message": "발행하려면 간편비밀번호를 입력해 주세요." } }
```

- `ApiResponse<T>`, `PageResponse<T>` record로 통일.
- 도메인 예외는 `BusinessException(ErrorCode)` 하나. `ErrorCode` enum: `(HttpStatus, code, message)`.
- `@RestControllerAdvice` 한 곳에서 처리. **스택트레이스·SQL·내부 경로를 응답에 노출하지 않는다.**
- 권한 없음은 `403`, 미인증은 `401`, sudo 모드 만료는 별도 코드(`PIN_REQUIRED`)로 구분해 프론트가 PIN 모달을 띄울 수 있게 한다.

### 7.3 엔티티

- `BaseTimeEntity`(createdAt/updatedAt) 상속, `@EntityListeners(AuditingEntityListener.class)`.
- 발행 대상 엔티티는 `Publishable`(status, publishedAt, publishedBy)을 갖는다.
- 연관관계는 전부 `fetch = LAZY` 명시.
- setter 금지. 상태 변경은 의도가 드러나는 메서드로 (`publish(account)`, `unpublish()`, `openSales()`).
- `ddl-auto`는 local에서도 `validate`. 스키마 변경은 Flyway만.
- 목록 조회 N+1은 `@EntityGraph` 또는 fetch join으로 해결하고, 왜 필요한지 주석 한 줄.
- **공개 조회 쿼리는 항상 `status = PUBLISHED`로 필터한다.** 이걸 서비스 계층 조건으로 흘리지 말고 리포지토리 메서드 이름에 박아라 (`findByStatusAndSlug`). 초안 유출은 이 프로젝트에서 가장 흔할 사고다.
- 콘텐츠는 거의 변하지 않는다. 공개 응답은 `@Cacheable`(Caffeine), 발행 시 `@CacheEvict`.

### 7.4 인증 상세

- **일반 회원**: OAuth2 Authorization Code 흐름. `SuccessHandler`는 `SignupPolicy`에게 이 소셜 계정을 받아들일지 물어본다(§3.2). `INVITE_ONLY`에서 `signup_allowlist`에 없으면 계정을 만들지 않고 권한 없음 화면으로 리다이렉트한다.
- **카카오 이메일은 없을 수 있다.** 사용자가 이메일 제공에 동의하지 않으면 `null`로 온다. 이메일을 계정 식별자로 쓰지 않는다. 식별은 `provider + provider_user_id`. 이메일이 필요한 기능(협업제안 회신)은 그 시점에 별도로 입력받는다.
- **관리자**: `POST /api/auth/admin/login`(아이디·비밀번호) → Spring Security `DaoAuthenticationProvider` + `BCryptPasswordEncoder`로 `admin_account`를 조회·검증한다. 카카오 플로우와 라우트만 다를 뿐, 응답으로 나가는 access/refresh 토큰의 모양과 이후 처리(§7.4 아래 규칙 전부)는 동일하다 — 프론트·리소스 서버 입장에서 로그인 수단 차이가 드러나지 않는다.
- access token은 응답 본문으로 주고 프론트 메모리에만 둔다. `Authorization: Bearer`로 전송. refresh token은 `HttpOnly; Secure; SameSite=Lax` 쿠키. **localStorage에 토큰을 저장하지 않는다.**
- access token 만료 15분, refresh 14일. 운영자 수가 적으니 짧게 잡아도 불편이 크지 않다.
- sudo 모드(PIN 재확인) 유효 시간은 서버가 관리한다. 클라이언트 타이머를 신뢰하지 않는다.

### 7.5 미디어 — 요구서에서 "매우 중요"로 명시된 항목

> "작품 고해상도 이미지가 많아질 것을 대비해 로딩 속도 최적화가 매우 중요함"

결제가 빠진 지금 **이 사이트의 기술적 난이도는 여기 하나에 집중된다.**

- 업로드는 **S3 presigned URL로 브라우저 → S3 직접 전송**. 20MB 파일이 Spring을 통과하게 만들지 않는다. 서버는 presign 발급과 메타데이터 저장만.
- presign 발급은 인증된 운영자만. 요청에 담긴 `contentType`·`contentLength`를 presign 조건에 포함시켜 다른 파일로 바꿔 올리지 못하게 한다. 계정당 시간당 발급 상한을 둔다.
- 업로드 완료 후 비동기로 파생본 생성. 원본은 별도 버킷에 보관하고 웹에 직접 노출하지 않는다.
  - 목록용: `640w / 960w / 1600w` WebP + JPEG fallback
  - 딥줌용: DZI 타일 피라미드 (OpenSeadragon)
  - LQIP: 24px 블러 placeholder를 DB에 base64로 저장 → CLS 0
- 확대/축소를 원본 20MB를 내려받아 CSS transform으로 확대하는 방식으로 구현하지 않는다. **타일 기반**이어야 한다.
- 이미지에는 항상 `width`/`height` 또는 `aspect-ratio`를 준다.
- 파일 검증은 확장자가 아니라 **매직 바이트**로. `image/*` 화이트리스트. 파일명은 UUID로 재작성.
- 파생본 생성 실패를 편집 화면에 명확히 표시한다. 조용히 실패하면 운영자는 올린 줄 알고 넘어간다.

### 7.6 다국어

유형 C(방한 외국인)가 명시되어 있으므로 **영문은 부가 기능이 아니라 요구사항**이다.

- UI 문자열: `frontend/src/locales/{ko,en}/*.json`. 컴포넌트에 한글 리터럴 하드코딩 금지.
- 콘텐츠 본문: `production_translation(production_id, locale, title, description, ...)` 테이블로 분리. `title_ko`, `title_en`을 나란히 두지 않는다.
- 편집 패널은 KO/EN을 탭으로 함께 제공한다. **한국어만 채우고 발행할 수 있게 하되, 발행 버튼 옆에 "영문 미작성" 경고를 표시**한다. 막지는 않는다 — 막으면 운영자가 발행을 미룬다.
- 번역 누락 시 한국어로 폴백하고, 편집 모드 목록에 누락 배지를 표시한다.
- URL은 `/en/...` 경로 프리픽스. `hreflang` 상호 참조 필수.
- **회차의 진행 언어(`spokenLanguage`)는 번역과 다른 문제다.** 영문 사이트를 보는 관광객에게 한국어로만 진행되는 회차를 예약 가능한 것처럼 보여주면 현장에서 사고가 난다. 회차마다 진행 언어와 자막·통역 제공 여부를 데이터로 갖고 영문 화면에 반드시 표시한다.
- 네이버 예약 페이지가 한국어 전용이라면, 영문 사용자에게 이동 전에 알리고 이메일 문의 경로를 함께 제시한다.

### 7.7 보안 일반

- 공개 API는 `GET` + 예외 두 개(협업 문의 등록, 예약 클릭 트래킹). 둘 다 IP당 레이트리밋.
- 문의 폼: 허니팟 필드 + IP당 분당 3회.
- 예약 클릭 트래킹은 인증이 없으므로 **어뜨한 값도 신뢰하지 않는다.** showing ID 존재 검증, UTM은 길이 제한 후 저장, 집계는 별도 배치로.
- CORS 허용 오리진은 환경별 yml에 명시. `*` 금지.
- 시크릿(카카오 클라이언트 시크릿, DB 비밀번호, S3 키, OpenAI 키)은 코드·yml·저장소에 넣지 않는다. 환경변수 주입.
- 개인정보: 문의 폼에 수집 항목·목적·보유기간 표시, 동의 여부·시각 저장, 3년 보관 후 삭제 스케줄러.
- 로그에 개인정보(이름·연락처·이메일 전문)를 남기지 않는다. 필요하면 마스킹.
- **감사 로그**: 모든 쓰기에 `account_id, action, target_type, target_id, before, after, ip, at`. 운영자가 소수라 사고 시 추적이 유일한 복구 수단이다.

---

## 8. 디자인

### 8.1 요구서에 못박힌 제약 (협상 불가)

> "작품 이미지가 돋보일 수 있도록 전체적인 배경은 무채색(블랙/화이트) 톤으로 구성하며, 미니멀하고 직관적인 레이아웃을 선호"

배경은 무채색이다. 컬러로 분위기를 만들 수 없으므로 **타이포·여백·이미지 크롭·모션**이 전부다. 정밀함이 곧 완성도다.

### 8.2 레퍼런스에서 가져올 것 / 버릴 것

가져온다
- 풀블리드 이미지 위 최소 텍스트. 이미지가 주인공, 문장은 조연.
- 편집형 세로 스토리텔링. 카드 그리드가 아니라 한 편의 글처럼 전개.
- **수동 줄바꿈 헤드라인.** 자동 줄바꿈에 맡기지 않고 의미 단위로 끊는다. 콘텐츠에 개행을 포함시켜 렌더한다. (해녀의부엌의 "해녀의 삶을 맛.보는 / 무대 위 2시간")
- 1px 아웃라인 CTA. 채운 버튼이 아니다.
- 데스크톱/모바일 이미지 에셋 분리. 크롭 비율이 달라야 한다.
- 영문 eyebrow + 국문 헤드라인 병치.

버린다
- 커머스 UI 잔여물(장바구니, 최근 본 상품, 국가별 배송 모달). 우리는 아무것도 팔지 않는다.
- 원칙은 **자동재생 캐러셀을 쓰지 않는다**(접근성·CLS 문제, 스크롤 기반 전개가 기본). **예외: 메인 히어로 하나만**, 2026-08-26 내부 협의로 허용 — 조건부다. 일시정지 버튼, 이전/다음 키보드 컨트롤, hover·focus 시 자동 정지, `prefers-reduced-motion`에서 타이머 자체를 켜지 않음을 전부 갖춰야 예외가 성립한다(구현: `components/section/HeroSlideshow.tsx`). 이 예외를 다른 곳(작품 목록, 아티스트 갤러리 등)으로 확대하지 않는다.
- 컬러 그라디언트. 요구서가 무채색을 요구한다. (히어로의 텍스트 가독성용 스크림은 `--c-black` 불투명도 그라디언트라 무채색 팔레트 안에 있다 — 색상 그라디언트가 아니다.)

### 8.3 컬러 토큰

```css
:root {
  /* 무채색 스케일 — 이 사이트의 전부 */
  --c-black:   #0B0B0C;  /* 다크 섹션 배경. 순수 검정 아님 */
  --c-ink:     #16171A;  /* 헤드라인, 본문 */
  --c-ink-60:  #5B5D62;  /* 부가 설명 */
  --c-ink-35:  #9A9CA1;  /* 캡션, 비활성 */
  --c-line:    #DEDEDC;  /* 1px 구분선 */
  --c-paper:   #F4F4F2;  /* 기본 배경 */
  --c-white:   #FFFFFF;

  /* 단 하나의 유채색 — 무대 조명의 텅스텐 */
  --c-spot:    #B98A3C;
}
```

`--c-spot`은 **딱 세 곳에만** 쓴다.
1. CTA hover / focus 테두리
2. 큐시트에서 현재 스크롤 지점 표시
3. 회차 캘린더의 `OPEN` 상태 표시

그림자는 쓰지 않는다. 깊이는 1px 선과 여백으로 만든다. 예외는 sticky 헤더의 `0 1px 0 var(--c-line)`.

**편집 모드 전용 색은 토큰에 추가하지 않는다.** 편집 UI는 공개 사이트의 무채색 시스템을 그대로 쓰고, 편집 가능 영역 표시는 `--c-spot` 1px 점선으로만 한다. 편집 모드를 위해 파랑·초록 같은 색을 새로 들이면 요구서의 무채색 원칙이 화면에서 깨진다.

### 8.4 타이포그래피

`frontend/public/fonts/`에 self-host, `font-display: swap`.

| 역할 | 서체 | 용도 |
|---|---|---|
| Display | **Hahmlet** (국문 고대비 세리프) | 히어로, 섹션 헤드라인. 절제해서 쓴다 |
| Body | **Pretendard Variable** | 본문, UI, 버튼, 편집 패널 |
| Utility | **IBM Plex Mono** | 시각(19:00), 코스 번호, 정원(14석), 영문 eyebrow |

고대비 세리프는 무대 프로그램북의 문법이고, 모노스페이스는 큐시트·타임코드의 문법이다. 둘 다 이 사업의 실제 재료에서 나온 선택이다.

```css
:root {
  --f-display: 'Hahmlet', serif;
  --f-body:    'Pretendard Variable', 'Pretendard', system-ui, sans-serif;
  --f-util:    'IBM Plex Mono', monospace;

  --t-hero:    clamp(2.25rem, 6vw, 4.75rem);
  --t-h1:      clamp(1.75rem, 4vw, 3rem);
  --t-h2:      clamp(1.375rem, 2.6vw, 2rem);
  --t-lead:    clamp(1rem, 1.4vw, 1.25rem);
  --t-body:    1rem;
  --t-caption: 0.8125rem;

  --lh-tight: 1.28;
  --lh-body:  1.75;
  --ls-wide:  0.14em;
}
```

- 국문 본문 `line-height`는 1.7 미만으로 내리지 않는다.
- Display는 `font-weight` 300~500만. 700 금지 — 고대비 세리프가 뭉개진다.
- 본문 한 줄 최대 폭 `38em`.
- 영문 페이지도 같은 스케일. 영문이 길어져 헤드라인이 3줄이 되는 경우를 반드시 확인한다.
- 편집 패널은 Display를 쓰지 않는다. 전부 Body + Utility.

### 8.5 레이아웃

```
데스크톱                            모바일
┌──────────────────────────────┐   ┌──────────┐
│[logo] 작품 소개 예약 협업제안   │   │[logo]  ☰ │  64/56px, 투명→불투명
│         리뷰 아티스트  KO/EN   │   │          │
├──────────────────────────────┤   ├──────────┤
│                              │   │          │
│    full-bleed 무대 이미지      │   │  hero    │  100svh
│    ── EYEBROW                │   │ (9:16    │
│       수동 줄바꿈된            │   │  크롭)   │
│       헤드라인 두 줄           │   │          │
│    [ 예약 보기 ]              │   │  [CTA]   │
├──────────────────────────────┤   ├──────────┤
│ │00:00  1st Course           │   │ 큐시트   │  ← 시그니처
│ │       요리명 / 장면 설명     │   │ 세로     │
│ │00:25  2nd Course           │   │ 스택     │
├──────────────────────────────┤   ├──────────┤
│  이미지 7col │ 텍스트 5col     │   │ 이미지    │  좌우 교차
│  텍스트 5col │ 이미지 7col     │   │ 텍스트    │
├──────────────────────────────┤   ├──────────┤
│  회차 캘린더 · 상태 · ↗ 예약    │   │ 캘린더    │
├──────────────────────────────┤   ├──────────┤
│  다크 섹션 (창작집단 소개)      │   │ 다크      │
├──────────────────────────────┤   ├──────────┤
│  footer: 연락처 / 사업자정보    │   │ footer   │
└──────────────────────────────┘   └──────────┘
```

```css
:root {
  --container: 1280px;
  --gutter-mobile: 20px;
  --gutter-desktop: 48px;
  --section-y-mobile: 72px;
  --section-y-desktop: 140px;
}
```

- **데스크톱 내비게이션이 6개 항목(작품·소개·예약·협업제안·리뷰·아티스트) + 언어 전환으로 늘었다(2026-08-26).** §8.1이 요구하는 "미니멀"과 조금씩 멀어지고 있다. 지금은 그대로 두되, 실제로 붙여보고 좁은 데스크톱 폭에서 줄바꿈되거나 너무 빽빽하면 협업제안·리뷰처럼 우선순위가 낮은 항목을 푸터나 "더보기" 메뉴로 옮기는 걸 고려한다.
- 12칼럼(desktop) / 4칼럼(mobile), `gap: 24px`.
- 히어로는 `100svh`. 모바일 주소창 때문에 `100vh`는 쓰지 않는다.
- `border-radius`는 `0`이 기본. 예외는 원형 아이콘 버튼.
- **편집 패널은 데스크톱 전용**(≥1024px). 모바일에서 편집 모드를 켜면 "PC에서 편집해 주세요" 안내를 띄운다. 20MB 이미지 업로드와 KO/EN 병행 입력을 모바일에서 지원하려 들면 비용이 폭발한다.

**브레이크포인트 (모바일 퍼스트, `min-width`로만 확장)**
```
mobile ~767  /  tablet 768~1023  /  desktop 1024~1439  /  wide 1440~
```

가로 스크롤 발생은 버그다. `overflow-x: hidden`으로 덮지 말고 원인을 고친다.

### 8.6 시그니처 — 큐시트 (Cue Sheet)

작품 상세 좌측에 1px 세로선이 콘텐츠를 관통하고, **코스 진행 순서를 타임코드와 함께** 표시한다. 스크롤이 각 코스 지점을 지날 때 해당 눈금과 라벨이 `--c-spot`으로 켜진다. 무대 조명 큐시트가 그대로 페이지 구조가 된다.

```
│00:00 ─ 1st Course   전복 미역국          막이 오르고
│00:25 ─ 2nd Course   성게 비빔            해녀의 첫 물질
│00:50 ─ INTERMISSION
```

- 이 사이트에서 화려한 지점은 여기 하나다. 나머지에는 장식을 더하지 않는다.
- 번호를 붙이는 이유는 **콘텐츠가 실제로 순서를 가진 시퀀스**이기 때문이다. 순서가 정보가 아닌 섹션(작품 목록 등)에는 `01 / 02` 마커를 붙이지 않는다.
- 코스 순서는 편집 패널에서 드래그로 바꿀 수 있어야 한다. 순서가 콘텐츠의 의미다.
- 모바일(≤767px)에서는 세로선을 숨기고 타임코드를 각 코스 블록 위에 얹는다.
- `position: sticky` + `transform: scaleY()`로 구현. 스크롤 값은 `requestAnimationFrame`으로 throttle하고 `scroll` 핸들러에서 레이아웃 값을 직접 읽지 않는다.

### 8.7 모션

```css
:root {
  --e-out: cubic-bezier(0.16, 1, 0.3, 1);
  --d-fast: 200ms;
  --d-base: 480ms;
  --d-slow: 900ms;
}
```

- 스크롤 리빌은 `useReveal` 훅(IntersectionObserver, `threshold: 0.15`, `once: true`) 하나로 통일.
- 리빌은 `opacity 0→1` + `translateY(24px→0)`뿐. 스케일·회전·블러 금지.
- 헤드라인은 줄 단위 `60ms` 스태거. 문자 단위 애니메이션 금지.
- 패럴랙스는 히어로 이미지 하나만, `translateY` 최대 8%.
- **편집 모드에서는 스크롤 리빌을 끈다.** 편집하려는 요소가 애니메이션으로 사라지면 작업이 불가능하다.
- **`prefers-reduced-motion: reduce`면 모든 transition/animation을 제거하고 최종 상태로 즉시 렌더한다.** 옵션이 아니다.

### 8.8 접근성 하한선 (PR 통과 조건)

- 본문 대비비 4.5:1, 대형 텍스트 3:1 이상. `--c-ink-35`는 캡션 전용. `--c-spot`(#B98A3C)은 흰 배경에서 본문 대비를 통과하지 못하므로 **텍스트 색으로 쓰지 않는다.**
- 모든 인터랙티브 요소에 보이는 포커스 링: `outline: 2px solid var(--c-spot); outline-offset: 3px`. `outline: none` 단독 금지.
- 이미지 `alt` 필수. 장식용은 `alt=""`. **편집 패널에 alt 입력 필드를 필수로 둔다.** 운영자가 안 쓰면 접근성이 무너지고, 이미지 중심 사이트라 SEO도 같이 무너진다.
- 모바일 메뉴·모달 열림 시 포커스 트랩 + `Esc` 닫기 + 배경 스크롤 락.
- 터치 타겟 최소 44×44px.
- 시맨틱 태그. `div`로 버튼을 만들지 않는다.
- 딥줌 뷰어에 키보드 조작(`+`/`-`/화살표)과 배율 초기화 버튼.
- 외부 링크는 스크린리더에도 알린다 (`aria-label`에 "새 창에서 열림" 포함).

### 8.9 카피

- 히어로 헤드라인은 두 줄, 줄당 12자 이내.
- 버튼은 결과를 말한다. "확인"·"제출" 대신 "네이버 예약으로 이동", "문의 보내기", "발행하기".
- 같은 동작은 흐름 전체에서 같은 이름을 유지한다. "발행하기"로 시작했으면 완료 토스트도 "발행되었습니다".
- 빈 상태는 사과하지 않고 다음 행동을 안내한다. "등록된 회차가 없습니다" → "다음 시즌 회차를 준비하고 있습니다."
- 에러는 무엇이 잘못됐고 어떻게 고치는지 말한다. "오류가 발생했습니다" 단독 금지. 업로드 실패는 파일 크기·형식 중 무엇이 문제인지 밝힌다.
- 형용사 나열 금지. 감각적·특별한·프리미엄·최고의 대신 구체적 사실. **"14석", "2시간", "9코스"가 "특별한 경험"보다 강하다.**

---

## 9. 프론트엔드 규칙

- 서버 데이터는 전부 TanStack Query. `useEffect` + `fetch` 조합 금지. 쿼리 키는 `api/queryKeys.ts`에 상수로 모은다.
- 콘텐츠는 거의 정적이므로 `staleTime`을 길게(작품·소개 1시간, 회차 목록 5분). **편집 모드에서는 `staleTime: 0`** — 방금 저장한 값이 안 보이면 운영자가 중복 저장한다.
- 로딩은 스피너 대신 스켈레톤 또는 LQIP 블러업.
- 함수 컴포넌트 + named export. 라우트 페이지만 `default export`.
- props 타입은 `type`으로 통일. `any` 금지, 불가피하면 `unknown` + 좁히기.
- 한 파일 200줄 넘기면 분리.
- `primitives/`는 도메인 지식을 갖지 않는다. `section/`은 가져도 된다.
- **`features/editing/`은 `React.lazy`로만 import한다.** 공개 컴포넌트가 편집 모듈을 정적 import하면 익명 방문자 번들에 섞여 들어간다. 빌드 후 번들 분석으로 확인한다.
- 공개 섹션 컴포넌트는 편집 여부를 모른다. 편집 어피던스는 `EditableSection` 래퍼가 주입한다.
- **`isAdmin` 같은 불리언을 컴포넌트에 흘리지 않는다.** 권한 판단은 `useCan('production:edit')` 하나로만 한다. 역할이 아니라 능력을 묻기 때문에, 나중에 `MEMBER`에게 `review:write`가 생겨도 훅 내부의 매핑 테이블 한 곳만 고치면 된다. `if (role === 'EDITOR')`가 컴포넌트에 박히면 그때 전수 수정이다.
- **색상·간격·폰트 크기 하드코딩 금지.** `var(--...)`만. 토큰에 없으면 토큰을 추가하고 커밋 메시지에 이유를 남긴다.
- `!important` 금지. 섹션 상하 여백은 `.section` 유틸 하나로만 관리하고 개별 컴포넌트에서 다시 주지 않는다.

### 반응형 이미지

```tsx
<picture>
  <source media="(min-width: 1024px)" srcSet={`${src}-1600.webp`} type="image/webp" />
  <source media="(min-width: 1024px)" srcSet={`${src}-1600.jpg`} />
  <source srcSet={`${src}-640.webp`} type="image/webp" />
  <img src={`${src}-640.jpg`} alt={alt} width={w} height={h} loading="lazy" decoding="async" />
</picture>
```

히어로만 `loading="eager"` + `fetchPriority="high"`. 나머지는 전부 lazy.

---

## 10. SEO / 성능

전환의 마지막 단계가 외부에 있으므로 **이 사이트의 유일한 성장 레버는 검색 유입**이다.

- **빌드 시 프리렌더링을 쓰지 않는다.** 콘텐츠가 라이브 사이트에서 편집·발행되므로, 빌드 시점에 굳힌 HTML은 발행 직후 구식이 된다. 대신 **Spring이 `index.html`을 서빙할 때 라우트별 메타 태그를 주입**한다. `MetaInjectionFilter`가 경로를 보고 DB에서 제목·설명·OG 이미지·`hreflang`을 조회해 `<head>`에 박는다.
  - **`index.html`은 클래스패스가 아니라 공유 볼륨의 파일에서 읽는다.** 프론트엔드가 독립적으로 배포되므로(§13.6), 부팅 시 한 번 읽어 메모리에 고정하면 프론트 배포 후 옛 에셋 해시를 계속 내려주고 사이트가 백지가 된다. mtime 변경 감지로 무효화한다.
  - 이 방식은 크롤러에 필요한 것(정확한 메타 + 정규 URL + JSON-LD)을 주면서 편집 즉시 반영된다.
  - 응답은 CDN에서 짧게(60초) 캐시하고 발행 시 무효화한다.
  - 본문 텍스트까지 서버 렌더가 필요해지면(작품 상세가 수십 개를 넘고 검색 순위가 중요해지면) Next.js 전환을 재검토한다. 지금 규모에서는 과하다.
- 라우트별 `<title>`, `description`, OG/Twitter. `hreflang` ko/en 상호 참조. **초안(`DRAFT`)은 절대 메타에 노출하지 않는다.**
- `sitemap.xml`·`robots.txt`는 백엔드가 동적 생성(발행된 작품·회차만). `/login`은 `Disallow` + `noindex`.
- JSON-LD: `Organization`, `TheaterEvent`(회차 — 일시·장소·`offers.url`에 예약 링크), `Restaurant`. 예약 링크를 `offers`에 넣으면 검색 결과에서 바로 예약으로 갈 수 있다.
- 목표: LCP < 2.5s, CLS < 0.1, **익명 방문자 초기 JS < 180KB(gzip)**. OpenSeadragon과 `features/editing/`은 동적 import로 분리한다. 이 수치는 편집 모듈이 빠진 상태에서 측정한다.
- 폰트는 subset(한글 상용 2350자 + Latin)으로 줄여 self-host.
- 네이버 서치어드바이저 등록 + sitemap 제출. 예약이 네이버 예약에 있으므로 네이버 생태계 정합성이 실적에 직결된다.

---

## 11. 스타일 · 테스트 · 커밋

**Backend** — 4스페이스. 생성자 주입만, 필드 주입 금지.
테스트: 서비스는 JUnit5 + Mockito + AssertJ, 컨트롤러는 `@WebMvcTest`, 리포지토리는 `@DataJpaTest`.

반드시 테스트할 것:
- **모든 `*EditController` 엔드포인트에 대해 익명·비허용목록 계정으로 403이 나는지.** 이 테스트가 없으면 새 엔드포인트를 추가할 때 권한을 빠뜨린다. 리플렉션으로 `*EditController`를 순회해 `@PreAuthorize` 누락을 잡는 아키텍처 테스트를 하나 둔다.
- `DRAFT` 콘텐츠가 공개 조회 API에 절대 안 나오는지.
- presign 발급이 인증 없이는 실패하는지, `contentLength` 상한을 넘기면 거부되는지.
- PIN 5회 실패 잠금, sudo 모드 만료.
- 회차 상태 전이, 예약 URL 호스트 검증, 번역 폴백.

**Frontend** — 2스페이스, 세미콜론. ESLint + Prettier. Vitest + RTL.
반드시 테스트할 것: `useReveal`, `ResponsiveImage`, 문의 폼 검증, i18n 폴백, 예약 버튼 비활성 조건, `EditableSection`이 비인증 상태에서 아무것도 렌더하지 않는지. 시각적 스타일은 테스트하지 않는다.

**커밋** — Conventional Commits(`feat:`, `fix:`, `refactor:`, `style:`, `docs:`, `test:`, `chore:`). 한 커밋 한 관심사. 백엔드/프론트엔드 변경을 한 커밋에 섞지 않는다. 본문에 **왜** 그렇게 했는지 한 줄.

---

## 12. Claude Code 작업 규칙

**시작 전**
1. 관련 파일을 먼저 읽는다. 구조 확인 없이 새 파일을 만들지 않는다.
2. 3개 파일 이상 건드리면 계획을 먼저 제시하고 승인을 받는다.
3. 모호하면 추측하지 말고 질문한다. 특히 회차 데이터 구조, 예약 URL 매핑, 번역 운영 주체, 편집 권한 범위.

**작업 중**
- 새 의존성은 먼저 물어본다. §5 "도입하지 않는다"에 있는 건 물어보지도 않는다.
- **쓰기 엔드포인트를 추가하면 같은 커밋에 `@PreAuthorize`와 403 테스트를 함께 넣는다.** 나중에 붙이지 않는다.
- **공개 조회 쿼리를 추가하면 `status = PUBLISHED` 필터를 같은 줄에서 확인한다.**
- 기존 파일 수정 시 관계없는 코드를 리포맷하지 않는다. diff를 작게.
- 색상·폰트·여백이 필요하면 `tokens.css`를 먼저 확인한다.
- `TODO`, `FIXME`, 주석 처리된 죽은 코드를 남기지 않는다.
- "이 파일은 ~를 담당합니다" 같은 설명 주석 금지. 주석은 why에만.

**작업 후**
- `./gradlew build`, `pnpm build && pnpm lint` 통과 확인.
- 편집 관련 작업이면 **번들 분석으로 `features/editing/`이 익명 청크에 없는지 확인**한다.
- 변경 사항을 3줄 이내로 요약. 코드를 다시 붙여넣지 않는다.

**절대 하지 말 것**
- 프론트엔드 조건부 렌더링을 권한 통제로 취급하지 않는다. 서버가 막지 않으면 막힌 게 아니다.
- 결제·좌석 차감 로직을 만들지 않는다. 범위 밖이고, 있으면 네이버 예약과 진실이 둘로 갈린다.
- 잔여석 숫자를 어디에도 표시하지 않는다.
- 관심 작품(찜)을 구현하지 않는다 — 요구서·§2 어디에도 없는 기능이다. 단 **"일반 회원은 존재하지 않는다"를 코드에 박지도 않는다.** 계정 엔티티를 운영자 전용으로 이름 짓거나, 단일 `role` 컬럼을 쓰거나, 공개 DTO에 뷰어 종속 필드를 섞는 것이 그 위반이다.
  - (2026-08-26 수정: 이 줄은 원래 "일반 회원가입 화면·리뷰·커뮤니티"도 금지 목록에 넣고 있었다. 그런데 §2 기능표·§3.2·§3.6은 회원가입과 커뮤니티(=리뷰, §3.6)를 기능 1·7로 명시하고 있어 원래부터 문서 내부 모순이었다. 회원가입·리뷰 둘 다 정식 기능이므로 금지 목록에서 뺐다.)
- 요청하지 않은 기능을 "있으면 좋을 것 같아서" 추가하지 않는다.
- 테스트를 통과시키기 위해 테스트를 약화시키지 않는다.
- 시크릿을 코드·yml에 넣지 않는다.

---

## 13. 인프라 / 배포

백엔드와 프론트엔드는 **하나의 Docker Compose로 함께 배포**한다. AWS 단일 EC2에서 돌린다.

### 13.1 컨테이너 구성

```
docker-compose.yml
├── nginx     : 엣지. 정적 에셋 서빙 + HTML·API 프록시
└── backend   : Spring Boot (Java 17, JRE 이미지)

docker-compose.local.yml (override, 로컬 전용)
└── mysql     : 로컬 개발용. dev·prod는 RDS (§13.4)

빌드 전용 스테이지 (런타임 컨테이너 아님)
└── frontend  : node 이미지에서 pnpm build → dist를 공유 볼륨에 출력
```

**프론트엔드는 런타임 컨테이너가 아니다.** SPA는 정적 파일이므로 별도 서버 프로세스가 필요 없다. `frontend`는 멀티스테이지 빌드로 `dist`를 만들어 named volume(`web-dist`)에 넣고 종료한다. nginx와 backend가 그 볼륨을 읽기 전용으로 마운트한다. Node 런타임 컨테이너를 상시 띄우는 건 2GB 인스턴스에서 순수 낭비다.

### 13.2 nginx 라우팅 — MetaInjectionFilter와 충돌하지 않게

§10에서 정한 대로 Spring이 `index.html`에 라우트별 메타를 주입한다. 따라서 **nginx가 `try_files`로 `index.html`을 직접 내려주면 SEO가 죽는다.** 라우팅을 이렇게 나눈다.

```nginx
# 1) 확장자가 있는 요청 = 빌드 산출물. nginx가 직접, 장기 캐시
location ~* \.(js|css|woff2|png|jpg|webp|svg|ico|json|txt|xml)$ {
    root /srv/web;
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
}

# 2) API
location /api/ { proxy_pass http://backend:8080; }

# 3) 나머지 전부 = HTML 문서 요청 → Spring이 메타 주입해서 반환
location / { proxy_pass http://backend:8080; }
```

- Vite 빌드 산출물은 파일명에 해시가 붙으므로 `immutable` 장기 캐시가 안전하다. `index.html`은 **절대 캐시하지 않는다**(Spring이 매번 만든다).
- `/actuator/**`는 nginx에 노출하지 않는다. `management.server.port`를 애플리케이션 포트와 분리하고 컨테이너 네트워크 안에서만 접근한다.
- gzip은 nginx에서. `text/html`, `application/json`, `js`, `css`, `svg`만.
- 업로드는 브라우저 → S3 직접이므로 nginx `client_max_body_size`를 키울 이유가 없다. `2m`으로 제한한다. 20MB가 nginx를 통과하려 한다면 그건 §7.5를 위반한 코드다.

### 13.3 CloudFront를 앞에 둔다

이미지용 CloudFront가 이미 필요하므로(§7.5), 사이트 전체를 CloudFront 뒤로 넣는다. 오리진 두 개:

| 경로 | 오리진 | 캐시 |
|---|---|---|
| `/media/*` | S3 (이미지 파생본) | 장기 |
| 그 외 | EC2 (nginx) | 정적 에셋 장기 / HTML 60초 |

- TLS는 ACM 인증서로 CloudFront에서 종료한다. **인스턴스에 certbot을 두지 않는다** — 갱신 실패로 사이트가 죽는 흔한 사고를 없앤다.
- 발행(publish) 시 해당 경로 invalidation을 호출한다. §3.9의 발행 플로우에 포함시킨다.
- EC2 보안 그룹은 **CloudFront managed prefix list에서 오는 443만** 허용한다. 오리진 직접 접근을 막지 않으면 CloudFront를 우회해 캐시·WAF가 무의미해진다.
- 관리자 접속(`/login`, 편집 모드)도 같은 도메인을 쓴다. 별도 도메인·포트를 열지 않는다.

### 13.4 데이터베이스 — RDS for MySQL

프로덕션 DB는 **RDS for MySQL 8.0**이다. 컨테이너 MySQL은 로컬 개발에서만 쓴다.

- `docker-compose.yml`에 mysql 서비스를 두지 않는다. `docker-compose.local.yml`(override)에만 둔다. 프로덕션 컴포즈에 DB 컨테이너가 있으면 누군가 실수로 띄우고, 두 개의 진실이 생긴다.
- 인스턴스: `db.t4g.micro`, gp3 20GB, **단일 AZ**. 이 트래픽에 Multi-AZ는 과하다. 대신 단일 AZ의 대가를 알고 있어야 한다 — 유지보수·페일오버 시 수 분 다운타임이 발생한다. **유지보수 창을 새벽(예: 화 04:00~04:30 KST)으로 고정**하고, 오픈 직전 주에는 창을 열지 않는다.
- `publicly accessible = false`. 프라이빗 서브넷. 보안 그룹은 **EC2 인스턴스의 SG만** 3306 허용. `0.0.0.0/0`은 어떤 이유로도 열지 않는다.
- 개발자 로컬에서 프로덕션 DB를 봐야 하면 **SSM 포트 포워딩**으로 터널링한다. 퍼블릭 액세스를 잠깐 켜지 않는다. 켜면 반드시 켜둔 채로 잊는다.
- `deletion protection` 켠다. 자동 백업 7일 + PITR. 스냅샷은 오픈 직전과 콘텐츠 대량 입력 직후에 수동으로 한 번씩.

**파라미터 그룹** (기본값을 그대로 쓰지 않는다)
- `character_set_server = utf8mb4`, `collation_server = utf8mb4_0900_ai_ci`
- `time_zone = UTC`
- `slow_query_log = 1`, `long_query_time = 1` — 딥줌 메타·번역 조인 쿼리가 늘어나면 여기서 먼저 보인다

**애플리케이션 쪽 주의**
- 접속 정보는 `SPRING_DATASOURCE_URL` / `_USERNAME` / `_PASSWORD` 환경변수로만. compose 파일에 하드코딩 금지 (§13.7).
- TLS 필수. `sslMode=REQUIRED`로 시작하고, RDS CA 번들을 이미지에 넣어 `VERIFY_IDENTITY`까지 올린다. `useSSL=false`로 두지 않는다.
- **HikariCP 풀 크기를 크게 잡지 않는다.** `db.t4g.micro`의 `max_connections`는 60~90 수준이다. backend 컨테이너가 하나이므로 `maximum-pool-size: 10`으로 충분하다. 50 같은 값을 넣으면 RDS 쪽에서 먼저 터진다.
- 유지보수·페일오버로 커넥션이 끊기는 걸 정상 상황으로 취급한다. `connection-test-query` 대신 JDBC4 `isValid`, `max-lifetime`은 RDS `wait_timeout`보다 짧게(예: 25분).
- **RDS에는 SUPER 권한이 없다.** Flyway 마이그레이션에서 `SET GLOBAL`, 일부 트리거·함수 DDL이 실패한다. 마이그레이션은 순수 DDL과 DML로만 작성한다.
- DB 사용자는 애플리케이션용 계정 하나. 마스터 계정으로 애플리케이션이 접속하지 않는다. 권한은 해당 스키마의 `SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, REFERENCES`까지. `DROP DATABASE`가 가능한 계정으로 붙지 않는다.

**마이그레이션 규칙 — 단일 인스턴스이므로 이게 중요하다**

Flyway가 부팅 시 실행되고 backend 컨테이너가 하나뿐이라, 파괴적 마이그레이션 후 롤백하면 이전 이미지가 새 스키마에서 뜨지 못한다. **expand-contract로 나눈다.**

```
1차 배포: 컬럼 추가 (nullable) → 코드가 양쪽 다 읽음
2차 배포: 백필 + 새 컬럼만 사용
3차 배포: 옛 컬럼 삭제
```

- 컬럼 삭제·이름 변경·타입 축소를 **코드 변경과 같은 배포에 넣지 않는다.**
- `NOT NULL` 컬럼을 기본값 없이 추가하지 않는다. 기존 행이 있으면 실패한다.
- 대용량 테이블에 인덱스를 추가할 일이 생기면 `ALGORITHM=INPLACE`를 확인한다. 지금 규모에선 문제없지만 `booking_click`은 계속 쌓인다.

**타임존** — DB와 애플리케이션 모두 UTC. 회차 시각은 `Instant`로 저장하고 화면에서 `Asia/Seoul`로 렌더한다. 다국어 사이트에서 서버 타임존을 KST로 두면 언젠가 하루가 밀린다.

### 13.5 이미지 빌드

- **arm64로 빌드한다** (EC2 t4g = Graviton, 같은 성능에 20% 저렴). Apple Silicon 로컬과도 아키텍처가 맞는다. Intel 머신에서 빌드하면 `--platform linux/arm64`가 필요하고 QEMU 에뮬레이션으로 느려지므로, 그 경우는 CI에서만 빌드한다.
- backend 이미지는 JDK가 아니라 **JRE 베이스**(`eclipse-temurin:17-jre-alpine` 등). 빌드는 별도 스테이지에서.
- 컨테이너에서 JVM 힙을 기본값에 맡기지 않는다. `JAVA_OPTS: -XX:MaxRAMPercentage=60 -XX:+UseSerialGC`. 2GB 인스턴스에서 기본 G1은 과하다.
- `.dockerignore`에 `node_modules`, `build`, `.gradle`, `dist`. 없으면 빌드 컨텍스트가 수백 MB가 된다.

### 13.6 배포 흐름 — 파이프라인은 분리한다

**서버에서 빌드하지 않는다.** t4g.small(2GB)에서 Gradle 빌드와 Vite 빌드를 돌리면 OOM으로 죽거나 배포 중 사이트가 느려진다.

같은 컴포즈로 함께 돌더라도 **CI/CD는 백엔드와 프론트엔드를 분리한다.** 한 저장소, 두 워크플로, 경로 필터 방식이다.

```
repo/
├── backend/                → .github/workflows/backend.yml   (paths: backend/**)
├── frontend/               → .github/workflows/frontend.yml  (paths: frontend/**)
├── deploy/nginx.conf       → .github/workflows/deploy.yml    (paths: deploy/**, docker-compose*.yml)
├── docker-compose.yml      ┘  실행 편의상 저장소 루트에 둔다 (`docker compose up`에 -f 경로가 필요 없게).
└── docker-compose.local.yml   §13.1의 구성과 실제 위치가 다르다는 점에 주의 — 배포 워크플로 경로 필터가 이 둘을 함께 잡는다.
```

**backend.yml**
```
gradlew test → buildx (linux/arm64) → ECR push (tag=SHA)
            → SSM Run Command: docker compose up -d backend
```

**frontend.yml**
```
pnpm test && pnpm build → dist를 tar로 압축 → S3 (빌드 아티팩트 버킷)
            → SSM Run Command: 서버에서 내려받아 web-dist 볼륨에 전개
            → CloudFront invalidation (/ 와 /index.html 만)
```

프론트엔드 배포는 **backend 컨테이너를 재시작하지 않는다.** 정적 파일 교체와 캐시 무효화뿐이다. 카피 한 줄 수정에 Spring이 재시작되고 Flyway가 돌 이유가 없다.

#### 분리를 성립시키는 조건 (하나라도 빠지면 깨진다)

1. **backend가 `index.html`을 캐시하지 않는다.** `MetaInjectionFilter`(§10)는 `index.html`을 클래스패스가 아니라 **공유 볼륨의 파일에서 매 요청 읽는다**(내용 해시 기반으로 인메모리 캐시하되 파일 mtime 변경 시 무효화). 부팅 시 한 번 읽어 메모리에 들고 있으면 프론트 배포 후에도 옛 에셋 해시를 계속 내려주고, 그 순간 사이트가 백지가 된다. **이게 분리의 유일한 필수 조건이다.**
2. **에셋 디렉토리를 비우지 않는다. 덮어쓴다.** 배포 시 `rm -rf`로 지우면 그 순간 페이지를 열어둔 사용자가 지연 로딩 청크에서 404를 맞는다. Vite 산출물은 파일명에 해시가 있으므로 겹치지 않는다. **최근 3개 빌드분을 남기고** 그보다 오래된 해시 파일만 정리한다.
3. **API 변경은 가산적으로만(additive).** 파이프라인이 분리되면 프론트가 아직 배포되지 않은 엔드포인트를 호출하거나 그 반대가 된다. 필드 추가·엔드포인트 추가는 자유롭게, **필드 삭제·응답 형태 변경·필수 파라미터 추가는 backend 먼저 배포하고 한 사이클 뒤에** 프론트를 올린다. §13.4의 expand-contract를 API에도 적용한다.
4. **롤백은 짝을 맞춰서.** 프론트 롤백은 이전 빌드의 `index.html`을 되돌리는 것이고, 해당 해시 에셋이 아직 남아 있어야 한다(조건 2). 백엔드 롤백 시 Flyway 마이그레이션은 되돌아가지 않는다 — 그래서 파괴적 마이그레이션을 금지한다.

#### 공통 규칙

- 이미지 태그와 프론트 아티팩트 이름은 **커밋 SHA**. `latest`로 배포하지 않는다 — 롤백할 방법이 없어진다.
- SSH 키를 GitHub Secrets에 넣지 않는다. **SSM Session Manager / Run Command**로 22번 포트를 아예 닫는다. GitHub → AWS 인증은 **OIDC 역할**로. 장기 액세스 키를 Secrets에 두지 않는다.
- `deploy/**` 변경(컴포즈·nginx.conf)은 세 번째 워크플로로 분리하고 **수동 승인**을 붙인다. 여기가 잘못되면 전체가 죽는다.
- **backend 배포 시 수 초의 다운타임이 있다.** 단일 인스턴스에서 blue-green을 만들지 않는다. 배포는 트래픽이 적은 시간에 하고, 그 사실을 문서에 적어둔다. 프론트 배포는 다운타임이 없다.
- Flyway 마이그레이션은 애플리케이션 부팅 시 실행된다. **backend 컨테이너를 2개 이상 띄우지 않는다** — 동시 마이그레이션은 락 경합과 부분 적용을 만든다.
- 헬스체크: compose `healthcheck`에 `/actuator/health/readiness`. nginx는 backend가 healthy가 된 뒤 뜨도록 `depends_on: condition: service_healthy`.

#### 완전 분리가 필요해지면 (지금은 하지 않는다)

프론트를 EC2에서 완전히 떼려면 **에셋을 S3에 두고 CloudFront가 경로로 오리진을 나누는** 구성으로 간다. `/assets/*`, `/fonts/*` → S3, 나머지 HTML 문서 → EC2. 그러면 프론트 배포가 `aws s3 sync` 한 줄이 되고 서버를 건드리지 않는다.

- 다만 `index.html`은 여전히 Spring이 만들어야 한다(메타 주입). 프론트 CI가 `index.html`을 S3에 올리고 Spring이 그걸 읽어 주입하는 형태가 되는데, **HTML 응답 경로에 S3 호출이 끼어드는 새 실패 지점**이 생긴다.
- 얻는 것(프론트 배포가 서버와 무관해짐)에 비해 늘어나는 실패 모드가 크다. 배포 빈도가 하루 수 회를 넘거나 프론트·백엔드 담당자가 나뉠 때 다시 검토한다. **오픈 전에는 하지 않는다.**

### 13.7 시크릿과 IAM

- **AWS 액세스 키를 환경변수로 넣지 않는다.** EC2 **인스턴스 역할(IAM Role)** 로 S3·CloudFront·SSM 권한을 준다. 키가 없으면 유출될 것도 없다.
- S3 권한은 해당 버킷의 필요한 prefix로만 제한한다. `s3:*` 금지.
- 카카오 클라이언트 시크릿과 DB 비밀번호는 **SSM Parameter Store(SecureString)** 에 두고 배포 시 `.env`로 내린다. 파일 권한 `600`, git에 절대 커밋하지 않는다. `.env.example`만 저장소에 둔다.
- compose 파일 자체는 저장소에 커밋한다. 시크릿이 들어 있지 않아야 하므로 전부 `${VAR}` 참조로 쓴다.

### 13.8 로그와 디스크

`docker compose logs`는 로그 전략이 아니다. **디스크가 차서 죽는 게 이 구성의 가장 흔한 장애다.**

- 모든 서비스에 로그 로테이션을 명시한다.
  ```yaml
  logging:
    driver: json-file
    options: { max-size: "10m", max-file: "3" }
  ```
- 애플리케이션 로그는 CloudWatch Logs로 보낸다. 인스턴스가 죽으면 로그도 같이 죽으면 원인을 알 수 없다.
- `docker system prune`을 주간 cron으로. 이미지 태그가 커밋 SHA라 쌓인다.
- CloudWatch 알람 최소 3개: 디스크 사용률 80%, CPU 크레딧 소진, 헬스체크 실패.
- **감사 로그(§7.7)는 DB에 있고 컨테이너 로그와 별개다.** 컨테이너 로그 로테이션이 감사 기록을 지우지 않는다는 점을 확인한다.

### 13.9 환경 구분

| 환경 | 실행 | DB | 용도 |
|---|---|---|---|
| local | `compose -f docker-compose.yml -f docker-compose.local.yml` | 컨테이너 MySQL | 개발 |
| dev | EC2 | **RDS 동일 인스턴스, 별도 데이터베이스**(`fdt_dev`) | **지원사업 점검용.** 요구서가 요구하는 접속 URL과 점검 계정을 이 환경에서 제공 |
| prod | EC2 + CloudFront | RDS (`fdt_prod`) | 실서비스 |

dev용 RDS 인스턴스를 따로 띄우지 않는다. 같은 인스턴스에 데이터베이스를 분리하고 **DB 사용자도 분리**해서 각자 자기 스키마에만 권한을 준다. `fdt_dev` 계정이 `fdt_prod`를 볼 수 없어야 한다. 이걸 안 하면 dev에서 돌린 Flyway가 프로덕션 스키마를 건드릴 수 있다.

> 트레이드오프: 같은 인스턴스를 쓰므로 dev의 무거운 쿼리가 프로덕션 성능에 영향을 준다. 이 규모에선 수용 가능하지만, 점검 기간에 대량 데이터를 넣어 테스트할 계획이 있으면 그때는 인스턴스를 분리한다.

요구서 참고 조항에 "점검용 유효한 사용자 및 관리자 계정(ID/비밀번호) 제공 필수"가 있다. **관리자 쪽은 이미 해결되어 있다** — `admin_account`가 모든 환경(local/dev/prod)에서 항상 아이디·비밀번호 로그인이므로, dev에 점검용 관리자 계정 하나를 심어두면 그대로 제출 가능하다(§3.1). **일반 회원 쪽만 여전히 문제다** — 카카오 소셜 로그인만 있으면 점검자에게 회원 계정을 줄 수 없다. dev 환경에 한해 일반 회원도 로컬 계정(이메일+비밀번호) 로그인을 허용하는 프로필을 둔다(§3.2의 `local-login` 플래그). `prod` 프로필에서는 이 인증 방식이 빈으로 등록되지 않아야 한다.

---

## 14. 실행

```bash
# 전체 스택 (통합 확인용)
docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
docker compose logs -f backend
docker compose down          # 볼륨 유지
docker compose down -v       # DB까지 초기화

# 개발 중에는 컨테이너를 쓰지 않는다 (핫리로드가 사라진다)
docker compose -f docker-compose.local.yml up -d mysql   # 로컬 DB만 띄우고

cd backend  && ./gradlew bootRun --args='--spring.profiles.active=local'
cd frontend && pnpm install && pnpm dev   # :5173, /api → :8080 프록시

# 검증
cd backend  && ./gradlew test && ./gradlew build
cd frontend && pnpm build && pnpm lint && pnpm test
```

**컴포즈는 배포 단위이고 개발 환경이 아니다.** 매 수정마다 이미지를 다시 빌드하면 개발이 불가능하다. 컴포즈 전체 기동은 배포 전 통합 확인에만 쓴다.

---

## 15. 요구서(2026-08-21 판) 검토 결과 — 제출 전 정리 필요

**문서 내부 모순 (점검에서 그대로 지적된다)**
- [ ] **오픈일이 두 개다.** 개요는 `2026년 9월 21일`, 프로젝트 범위는 `9월 말/10월 초 (협의 중)`. 참고 조항이 "개발 및 실제 서비스 오픈 일정을 명확하게 기재"를 요구한다. 하나로 통일한다.
- [x] ~~가입 유형과 회원 유형을 문서에서 분리한다~~ — **2026-08-27 결정으로 무효화.** 회원 유형(관람객/예술인/기업) 축 자체를 없앴다(요구서 쪽도 함께 정리됨). 남은 작업은 「사용자 권한 및 유형」표를 **가입 유형(권한): 일반회원/관리자** 하나로만 정리하는 것뿐 — 기능 1 문구에서 "일반/기업/예술가" 선택 문구를 제거한다.
- [ ] **다국어가 핵심 기능 표에 없다.** 과업내용·프로젝트 범위에는 있는데 기능 표에는 항목이 없다. 점검은 기능 표 기준으로 대조하므로, 필수 기능이면 **9번 항목으로 추가**해야 한다.
- [ ] 「프로젝트 범위」 제목 옆에 깨진 문자(`浥%！ÿ`)가 2곳 있다. 제출 전 확인.

**요구사항과 타겟의 충돌**
- [ ] **카카오 단독 로그인 vs 관광객 타겟.** 타겟 고객에 "한국 유입 관광객"이 있고 커뮤니티가 필수 기능인데, 외국인은 카카오 계정이 없어 가입 자체가 불가능하다. 구글 추가를 권한다 — 애플과 달리 구현 부담이 작다(카카오와 같은 OAuth2 표준 흐름, 추가 코드는 설정 수준). 추가하지 않을 거면 **타겟에서 관광객을 빼거나, 관광객은 열람·예약만 하는 대상임을 문서에 명시**한다.
- [x] **관리자 점검 계정 문제는 2026-08-27 결정으로 해소됨.** 관리자가 카카오와 무관한 별도 아이디·비밀번호 계정(`admin_account`, §3.1)이 되면서, dev에 점검용 관리자 계정을 심어두는 것만으로 요구서의 "점검용 관리자 계정" 요건을 충족한다.
- [ ] **일반 회원 점검 계정은 여전히 문제로 남아있다.** 참고 조항이 요구하는 "점검용 유효한 사용자 계정(ID/비밀번호)" 중 회원 쪽은 카카오 소셜 로그인만으로 발급할 수 없다. §13.9의 로컬 로그인 프로필로 대응하되, **어느 환경에서 제공할지 문서에 URL과 함께 적어야 한다.**
- [ ] **네이버 예약 정보가 부족하다.** "외부 플랫폼 이용 시 사업자명, 서비스명 등 검색 가능한 정보 명시" 조항에 대해 지금은 "네이버 예약을 연결"까지만 적혀 있다. 네이버 예약상의 **업체명과 페이지 URL**을 명시한다.
- [ ] **정량 목표가 없다.** "데이터 셋 구축 시 구체적인 목표 명시" 조항. 작품 N개, 회차 N개, 아티스트 N명, 레시피 N건을 범위 표에 넣는다. 챗봇을 유지한다면 레시피 건수가 곧 RAG 코퍼스 규모다.

**대화에서 정리된 내용과 문서가 어긋난 부분** — 어느 쪽이 맞는지 확정 필요
- [x] 협업제안(기능 4), 아티스트(기능 6)는 이전에 "아마 빠질 것"이었으나 **문서에는 남아 있었다.** 2026-08-26에 둘 다 필수로 확정했고, 아티스트의 하트(좋아요)만 범위에서 뺐다(§3.8). 요구서 자체는 원래도 이 기능들을 포함하고 있었으니 요구서 갱신은 불필요.
- [ ] 커뮤니티(기능 7)가 새로 정식 편입되었다. 이에 따라 **일반 회원가입이 1차 오픈 범위**가 되었고, 이용약관·개인정보처리방침·탈퇴 경로가 오픈 전 필수가 되었다(§3.2). 이건 개발보다 문서 작업이 오래 걸린다.
- [x] ~~리뷰(기능 9)가 요구서에 없는 새 기능~~ — **정정: 새 기능이 아니다.** 처음엔 이렇게 잘못 정리했다가, 리뷰는 요구서 기능 7 "커뮤니티"를 사이트에서 부르는 이름일 뿐이라는 걸 확인했다(2026-08-26, §3.6). 요구서 갱신은 필요 없다. 다만 **사이트가 "리뷰"라고 부르는 것을 요구서·발표 자료에서는 "커뮤니티"라고 부른다는 점은 센터 보고 시 헷갈리지 않게 짚어야 한다.** 관리자가 남의 글을 원문 수정까지 할 수 있게 넓힌 것(§3.6)도 요구서의 "최소 운영 기능" 범위 안의 운영 정책일 뿐 새 기능은 아니라고 본다 — 이견이 있으면 확인한다.
- [x] **관리자 계정을 일반 회원 계정과 완전히 분리했다(2026-08-27).** 기존에는 관리자도 카카오로 가입한 뒤 `account_invite`로 승격되는 같은 `Account` 엔티티였다. 이제 관리자는 `admin_account`라는 별개 테이블·엔티티이고, 회원가입 절차 없이 `SUPER_ADMIN`이 아이디·비밀번호를 직접 발급한다(§3.1). 승격·초대 개념 자체가 사라졌다. 부수 효과로 §15의 "점검용 관리자 계정" 문제가 해소됐다(위 항목 참고).
- [x] **회원 유형(`AccountType`: 관람객/예술인/기업) 축을 완전히 제거했다(2026-08-27).** 가입 유형과 회원 유형을 분리하던 기존 설계(§1·§3.1) 자체를 걷어냈고, 계정 구분은 `Role`(일반회원/관리자) 하나뿐이다. 요구서 쪽도 함께 정리된 것으로 확인됨 — 「사용자 권한 및 유형」표·기능 1 문구에서 회원 유형 선택 항목을 빼야 한다(위 항목 참고). 협업제안 폼의 자동 채움 로직도 이 축에 의존하지 않는다(§3.7).
- [ ] **메인 히어로 자동재생 슬라이드가 2026-08-26에 §8.2 원칙(자동재생 캐러셀 금지)의 예외로 허용됐다.** 접근성 보완 조건(§8.2 참고)을 지키는 한 디자인 리스크는 낮지만, 요구서 검수 시 "일관된 디자인 가이드"를 확인한다면 이 예외를 설명할 수 있어야 한다.

**설계 확정**
- [ ] 네이버 예약이 **회차별 딥링크**를 주는지. 업체 페이지 단위 URL만 가능하면 사용자가 넘어가서 날짜를 다시 골라야 하므로 캘린더 UX를 다르게 설계해야 한다.
- [ ] 관리자 인원과 역할 분리 필요 여부. 3명 이하면 `SUPER_ADMIN`/`EDITOR` 구분 없이 단일 역할로 단순화하는 게 낫다.
- [ ] 회차 판매 상태를 누가·얼마나 자주 갱신하는지. 수동이면 1클릭 토글을 캘린더 위에 바로 둔다.
- [ ] 회차 진행 언어와 통역·자막 제공 여부. 영문 화면 설계의 전제다.
- [ ] 영문 콘텐츠를 누가 쓰는지. 번역 미완으로 오픈하면 관광객 타겟이 사라진다.
- [x] 리뷰(요구서상 커뮤니티) 탈퇴 시 게시글 처리 정책 — **익명화 보존으로 확정**(2026-08-26, §3.1·§3.2). 삭제하지 않는다.

**인프라 확정**
- [ ] RDS를 EC2와 같은 VPC·같은 AZ에 둘 것. 다른 AZ면 AZ 간 트래픽 비용과 레이턴시가 붙는다.
- [ ] 도메인과 ACM 인증서 발급. CloudFront용 인증서는 **us-east-1에서 발급**해야 한다(서울 리전 아님).
- [ ] EC2 인스턴스 타입. t4g.small(2GB)로 시작 가능하지만 이미지 파생본 생성(§7.5)을 같은 인스턴스에서 돌리면 메모리가 부족할 수 있다. 별도 워커나 Lambda로 뺄지 함께 결정.
- [ ] AWS 계정 주체와 비용 부담 방식. 지원사업 정산 대상인지 확인 필요.

---

## 16. 차후 과제 — RAG AI 챗봇 (요구서 기능 8)

**지금 구현하지 않는다.** 단 아래 사실을 알고 미루는 것과 모르고 미루는 것은 다르다.

### 미루기 전에 반드시 처리할 것

요구서에는 챗봇이 **세 곳**에 적혀 있다 — 과업내용, 핵심 기능 8번, 프로젝트 범위(「형태」에 `text-embedding-3-small`과 gpt 모델까지 명시). MVP 점검 기준은 "사전 협의 없는 임의 기능 축소" 불가이고, "개발 목록에 명시되어 있으나 실제 구현되지 않은 경우 완료 확인이 어려울 수 있음"이라고 못박고 있다.

따라서 선택지는 둘뿐이다.

1. **문서에서 뺀다** — 세 곳 모두. 단 이 경우 **이 사업의 AI 적용 실적이 0이 된다.** 참고 조항의 「AI 적용의 구체성」에 대응할 항목이 사라진다.
2. **구현한다** — 그러면 지금 미루는 것이 아니라 4단계 이후 일정에 넣는 것이다.

빼기로 한다면, AI 실적을 대신할 후보는 **다국어 초벌 번역 + 사람 검수 파이프라인**이다. 영문이 이미 필수 기능이고, 편집 패널에 검수 UI가 이미 있고, 사용 모델·프롬프트·검수 절차를 문서로 남길 수 있어 "어떤 AI 모델을 어떠한 방식으로 적용했는지 상세 명시" 요구를 정직하게 충족한다.

### 구현하게 될 경우의 설계 메모

- **벡터 DB를 도입하지 않는다.** 코퍼스가 작품 수십 건 + 레시피 수십~수백 건이다. 임베딩을 MySQL에 `JSON`/`BLOB`으로 저장하고 애플리케이션 메모리에서 코사인 유사도를 계산하면 충분하다. 이 규모에서 pgvector·OpenSearch·Qdrant는 운영 부담만 늘린다. RDS MySQL 8.0에는 네이티브 벡터 타입이 없다는 점도 같은 결론을 가리킨다.
- **청크는 콘텐츠 구조를 따른다.** 작품 설명, 코스별 요리 설명, 레시피를 각각 한 청크로. 고정 길이로 자르지 않는다 — 이미 의미 단위로 나뉜 데이터를 임의로 자를 이유가 없다.
- 임베딩 생성은 **발행(publish) 시점**에 트리거한다(§3.9). 질문할 때마다 만들지 않는다.
- **다국어가 얽힌다.** 한국어 코퍼스에 영어로 질문이 들어온다. 질문을 번역해 검색할지, 번역본까지 임베딩할지 결정해야 한다. 코퍼스가 작으므로 **번역본도 함께 임베딩**하는 쪽이 단순하다.
- 참고 조항: **"단순한 데이터 비교 및 유사도 검증 방식은 AI 적용 기준으로 간주하기 어려움."** 검색만 하고 끝내면 안 되고, 검색 결과를 근거로 gpt가 답변을 생성하는 단계까지 있어야 한다. 요구서에 명시된 구성이 이미 이 조건을 만족한다.
- **모르면 모른다고 답하게 만든다.** 레시피와 작품 정보는 사실 정보다. 근거 청크의 유사도가 임계값 미만이면 생성하지 말고 문의 경로를 안내한다. 없는 코스를 지어내는 챗봇은 현장 CS 사고가 된다.
- 비용 보호: 세션당·IP당 질문 수 제한, 응답 토큰 상한, 동일 질문 캐시. OpenAI 키는 SSM Parameter Store(§13.7).
